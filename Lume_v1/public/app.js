(() => {
  const { categories, products } = window.CASA_AURORA_DATA;
  const money = (value) => new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }).format(value);
  const loadJSON = (key, fallback) => { try { const value = JSON.parse(localStorage.getItem(key)); return value ?? fallback; } catch { localStorage.removeItem(key); return fallback; } };
  const loadCart = () => loadJSON('lume-cart', []).filter((item) => item && typeof item.productId === 'string' && products.some((product) => product.id === item.productId) && Number.isInteger(Number(item.quantity)) && Number(item.quantity) > 0).map((item) => ({ productId: item.productId, quantity: Math.min(99, Number(item.quantity)) }));
  const state = { category: 'todas', search: '', page: 1, pageSize: 8, sort: 'featured', maxPrice: Math.max(...products.map((product) => product.price)), cart: loadCart(), service: true, theme: localStorage.getItem('lume-theme') || 'light', history: loadJSON('lume-history', []) };
  const $ = (selector) => document.querySelector(selector);
  const toast = (message) => { const node = $('#toast'); node.textContent = message; node.classList.add('show'); setTimeout(() => node.classList.remove('show'), 2400); };
  const getCategory = (id) => categories.find((category) => category.id === id);
  const cartDetails = () => state.cart.map((item) => ({ ...item, product: products.find((product) => product.id === item.productId) })).filter((item) => item.product);
  const totals = (couponCode = '') => { const subtotal = cartDetails().reduce((sum, item) => sum + item.product.price * item.quantity, 0); const discount = couponCode.trim().toUpperCase() === 'LUME10' ? subtotal * 0.1 : 0; const afterDiscount = subtotal - discount; const service = state.service ? afterDiscount * 0.1 : 0; return { subtotal, discount, service, total: afterDiscount + service, couponCode: discount ? 'LUME10' : '' }; };
  const saveCart = () => { localStorage.setItem('lume-cart', JSON.stringify(state.cart)); renderCart(); };
  const saveHistory = (order) => { state.history = [order, ...state.history.filter((saved) => saved.reference !== order.reference)].slice(0, 8); localStorage.setItem('lume-history', JSON.stringify(state.history)); };
  function filteredProducts() { const query = state.search.trim().toLocaleLowerCase('pt-BR'); const list = products.filter((product) => { const categoryMatch = state.category === 'todas' || product.category === state.category; const queryMatch = !query || `${product.name} ${product.description}`.toLocaleLowerCase('pt-BR').includes(query); return categoryMatch && queryMatch && product.price <= state.maxPrice; }); return list.sort((a, b) => state.sort === 'price-asc' ? a.price - b.price : state.sort === 'price-desc' ? b.price - a.price : a.id.localeCompare(b.id)); }
  function renderCategories() { $('#categories').innerHTML = [`<button class="${state.category === 'todas' ? 'active' : ''}" data-category="todas">Todos</button>`, ...categories.map((category) => `<button class="${state.category === category.id ? 'active' : ''}" data-category="${category.id}">${category.label}</button>`)].join(''); $('#categories').querySelectorAll('button').forEach((button) => button.addEventListener('click', () => { state.category = button.dataset.category; state.page = 1; renderProducts(); renderCategories(); })); }
  function renderProducts() { const list = filteredProducts(); const pages = Math.max(1, Math.ceil(list.length / state.pageSize)); state.page = Math.min(state.page, pages); const visible = list.slice((state.page - 1) * state.pageSize, state.page * state.pageSize); $('#result-count').textContent = `${list.length} itens`; $('#product-grid').innerHTML = visible.map((product) => `<article class="product"><span class="category">${getCategory(product.category)?.label || product.category}</span><h3>${product.name}</h3><p>${product.description}</p><footer><strong>${money(product.price)}</strong><button data-add="${product.id}">Adicionar ao pedido</button></footer></article>`).join(''); $('#product-grid').querySelectorAll('[data-add]').forEach((button) => button.addEventListener('click', () => add(button.dataset.add))); $('#pagination').innerHTML = pages > 1 ? `<button ${state.page === 1 ? 'disabled' : ''} data-page="${state.page - 1}">←</button><span>Página ${state.page} de ${pages}</span><button ${state.page === pages ? 'disabled' : ''} data-page="${state.page + 1}">→</button>` : ''; $('#pagination').querySelectorAll('[data-page]').forEach((button) => button.addEventListener('click', () => { state.page = Number(button.dataset.page); renderProducts(); })); }
  function add(id) { const existing = state.cart.find((item) => item.productId === id); if (existing) { if (existing.quantity >= 99) return toast('A quantidade máxima por produto é 99.'); existing.quantity += 1; } else state.cart.push({ productId: id, quantity: 1 }); saveCart(); toast('Item adicionado ao pedido.'); }
  function change(id, amount) { const item = state.cart.find((entry) => entry.productId === id); if (!item) return; item.quantity = Math.min(99, item.quantity + amount); state.cart = state.cart.filter((entry) => entry.quantity > 0); saveCart(); }
  function renderCart() { const details = cartDetails(); const { subtotal, service, total } = totals($('#coupon-code')?.value || ''); $('#cart-count').textContent = state.cart.reduce((sum, item) => sum + item.quantity, 0); $('#cart-items').innerHTML = details.length ? details.map(({ product, quantity }) => `<div class="cart-row"><div><strong>${product.name}</strong><small>${quantity} × ${money(product.price)} cada</small><small class="item-total">Total do item: ${money(product.price * quantity)}</small></div><div class="qty"><button aria-label="Diminuir ${product.name}" data-change="${product.id}" data-amount="-1">−</button><b>${quantity}</b><button aria-label="Aumentar ${product.name}" data-change="${product.id}" data-amount="1">+</button><button class="remove" aria-label="Remover ${product.name}" data-remove="${product.id}">×</button></div></div>`).join('') : '<p class="empty">Seu pedido está vazio.<br>Escolha algo no cardápio.</p>'; $('#cart-items').querySelectorAll('[data-change]').forEach((button) => button.addEventListener('click', () => change(button.dataset.change, Number(button.dataset.amount)))); $('#cart-items').querySelectorAll('[data-remove]').forEach((button) => button.addEventListener('click', () => { state.cart = state.cart.filter((item) => item.productId !== button.dataset.remove); saveCart(); })); $('#subtotal').textContent = money(subtotal); $('#discount').textContent = money(totals($('#coupon-code')?.value || '').discount); $('#service').textContent = state.service ? money(service) : '—'; $('#total').textContent = money(total); }
  function showHistory() { const modal = document.createElement('div'); modal.className = 'checkout-backdrop'; modal.innerHTML = `<section class="checkout-modal history-modal"><button type="button" class="close" aria-label="Fechar">×</button><span class="eyebrow">Memória da casa</span><h2>Histórico<br><em>de pedidos.</em></h2><div class="history-list"></div></section>`; const list = modal.querySelector('.history-list'); list.innerHTML = state.history.length ? state.history.map((order) => `<article><strong>${order.reference}</strong><span>${order.customerName} · Mesa ${order.tableNumber}</span><b>${money(order.total)}</b></article>`).join('') : '<p class="empty">Nenhum pedido salvo neste navegador.</p>'; }
  function openCheckout() { if (!state.cart.length) return toast('Adicione ao menos um item ao pedido.'); const modal = document.createElement('div'); modal.className = 'checkout-backdrop'; modal.innerHTML = `<form class="checkout-modal" id="checkout-form"><button type="button" class="close" id="close-checkout">×</button><span class="eyebrow">Finalização</span><h2>Direto para<br><em>a sua mesa.</em></h2><p>Informe seus dados e escolha a forma de pagamento.</p><label>Seu nome<input name="name" required minlength="2" placeholder="Ex.: Marina"></label><label>Número da mesa <small>(1 a 50)</small><input name="table" type="number" min="1" max="50" step="1" required placeholder="Ex.: 12"></label><label>Observações do pedido<textarea name="notes" maxlength="500" placeholder="Ex.: sem cebola, ponto da carne..."></textarea></label><label>Cupom de desconto<input name="coupon" placeholder="Digite LUME10"></label><fieldset><legend>Forma de pagamento</legend><label><input type="radio" name="payment" value="card" checked> Cartão</label><label><input type="radio" name="payment" value="pix"> PIX</label><label><input type="radio" name="payment" value="cash"> Dinheiro</label></fieldset><button class="button full" type="submit">Finalizar pedido →</button><p class="api-status" id="api-status"></p></form>`; document.body.append(modal); modal.querySelector('input[name="coupon"]').value = $("#coupon-code").value; $('#close-checkout').addEventListener('click', () => modal.remove()); $('#checkout-form').addEventListener('submit', async (event) => { event.preventDefault(); const form = new FormData(event.currentTarget); const table = Number(form.get('table')); const couponCode = String(form.get('coupon') || '').trim().toUpperCase(); if (!Number.isInteger(table) || table < 1 || table > 50) return toast('Informe uma mesa entre 1 e 50.'); if (couponCode && couponCode !== 'LUME10') return toast('Cupom inválido. Tente LUME10.'); const button = event.currentTarget.querySelector('button[type="submit"]'); button.disabled = true; button.textContent = 'Enviando...'; try { const response = await fetch('/api/orders', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ customerName: form.get('name'), tableNumber: table, paymentMethod: form.get('payment'), serviceFeeEnabled: state.service, couponCode, notes: form.get('notes'), items: state.cart }) }); const payload = await response.json(); if (!response.ok) throw new Error(payload.error || 'Não foi possível registrar o pedido.'); state.cart = []; saveCart(); modal.remove(); saveHistory(payload.order); showConfirmation(payload.order); } catch (error) { button.disabled = false; button.textContent = 'Finalizar pedido →'; $('#api-status').textContent = error.message; } }); }
  function showConfirmation(order) { const section = $('#confirmation'); section.classList.remove('hidden'); section.replaceChildren(); const eyebrow = document.createElement('span'); eyebrow.className = 'eyebrow'; eyebrow.textContent = 'Pedido confirmado'; const reference = document.createElement('h2'); reference.textContent = order.reference; const message = document.createElement('p'); message.textContent = `${order.customerName}, seu pedido foi enviado para a mesa ${order.tableNumber}.`; const summary = document.createElement('div'); summary.className = 'confirmation-summary'; const title = document.createElement('h3'); title.textContent = 'Resumo do pedido'; const items = document.createElement('ul'); items.className = 'confirmation-items'; order.items.forEach((item) => { const row = document.createElement('li'); const description = document.createElement('span'); description.textContent = `${item.quantity} × ${item.name} (${money(item.unitPrice)} cada)`; const amount = document.createElement('strong'); amount.textContent = money(item.itemTotal); row.append(description, amount); items.append(row); }); const totalsBox = document.createElement('div'); totalsBox.className = 'confirmation-totals'; const addTotal = (label, value, emphasis = false) => { const row = document.createElement(emphasis ? 'strong' : 'span'); row.textContent = label; const amount = document.createElement('b'); amount.textContent = money(value); row.append(amount); totalsBox.append(row); }; addTotal('Subtotal', order.subtotal); if (order.discount > 0) addTotal(`Desconto ${order.couponCode}`, -order.discount); addTotal(order.serviceFee > 0 ? 'Garçom (10%)' : 'Garçom (10%) — não cobrado', order.serviceFee); addTotal('Total', order.total, true); summary.append(title, items, totalsBox); if (order.notes) { const notes = document.createElement('p'); notes.className = 'confirmation-notes'; notes.textContent = `Observações: ${order.notes}`; summary.append(notes); } section.append(eyebrow, reference, message, summary); section.scrollIntoView({ behavior: 'smooth' }); }
  function applyTheme() { document.body.dataset.theme = state.theme; localStorage.setItem('lume-theme', state.theme); $('#theme-toggle').textContent = state.theme === 'dark' ? '☼ Modo claro' : '☾ Modo escuro'; }
  $('#search').addEventListener('input', (event) => { state.search = event.target.value; state.page = 1; renderProducts(); }); $('#sort-products').addEventListener('change', (event) => { state.sort = event.target.value; state.page = 1; renderProducts(); }); $('#max-price').addEventListener('input', (event) => { state.maxPrice = Number(event.target.value); $('#max-price-value').textContent = money(state.maxPrice); state.page = 1; renderProducts(); }); $('#service-fee').addEventListener('change', (event) => { state.service = event.target.checked; renderCart(); }); $('#coupon-code').addEventListener('input', () => renderCart()); $('#checkout-button').addEventListener('click', openCheckout); $('#theme-toggle').addEventListener('click', () => { state.theme = state.theme === 'dark' ? 'light' : 'dark'; applyTheme(); }); $('#history-button').addEventListener('click', showHistory); $('#clear-history').addEventListener('click', () => { state.history = []; localStorage.removeItem('lume-history'); toast('Histórico apagado neste navegador.'); }); $('#max-price').max = Math.ceil(Math.max(...products.map((product) => product.price))); $('#max-price').value = state.maxPrice; $('#max-price-value').textContent = money(state.maxPrice); applyTheme(); renderCategories(); renderProducts(); renderCart();
})();
